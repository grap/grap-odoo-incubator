In odoo, by default, all user can see messages ("Calendar" menu entry).
This feature is not interesting in some instances.

This module so creates a new group 'See Calendar Menu' to display this
entry menu and users are not member of this group by default.
