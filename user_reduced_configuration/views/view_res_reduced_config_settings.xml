<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_reduced_config_settings_view_form" model="ir.ui.view">
        <field name="model">res.reduced.config.settings</field>
        <field name="mode">primary</field>
        <field name="priority" eval="1000"/>
        <field name="inherit_id" ref="base_setup.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[hasclass('settings')]" position="inside" />

        </field>
    </record>

    <record id="action_res_reduced_config_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.reduced.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="context">{'module' : 'general_settings', 'bin_size': False}</field>
    </record>

</odoo>
