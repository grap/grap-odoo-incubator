/*
Copyright (C) 2018-Today GRAP (http://www.grap.coop)
@author: Sylvain LE GAL (https://twitter.com/legalsylvain)
License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl.html).
*/

"use strict";


openerp.pos_journal_image = function(instance){
    var module = instance.point_of_sale;

    // var _super_barcode_error_action = module.ScreenWidget.prototype.barcode_error_action;
    // module.ScreenWidget.include({

//        barcode_error_action: function(code){
//            var reg = new RegExp(this.pos.config.barcode_warning_regexpr);
//            res = code.code.match(reg);
//            if (res && res.length === 1 && res[0] === code.code){
//                _super_barcode_error_action.apply(this, arguments);
//            }
//        },

    // });

};
